import { createSchemaTypes } from "@laptopclub/foundation-cms";

// Add site-specific schemas and page block type names here.
export const siteSchemaTypes = createSchemaTypes({
  additionalPageBlockTypes: [],
  additionalTypes: []
});
